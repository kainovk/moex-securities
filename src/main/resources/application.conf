server {
  host = "localhost"
  port = 8080
}

database {
  driver = "org.postgresql.Driver"
  url = "jdbc:postgresql://localhost:5432/moex_securities"
  user = "postgres"
  password = "root"
  thread-pool-size = 32
}
